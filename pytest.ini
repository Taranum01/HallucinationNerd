[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers
markers =
    benchmark: marks tests that require OpenAI API credits (deselect with '-m "not benchmark"')
    integration: marks tests that hit external services (arXiv, PubMed, etc.)
filterwarnings =
    ignore::DeprecationWarning
